# version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: jaguar_backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:16
    container_name: jaguar_db
    environment:
      POSTGRES_DB: jaguar
      POSTGRES_USER: jaguar
      POSTGRES_PASSWORD: jaguar
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres_data:
